# Historial de Auditorías — GanadoSmart Backend
## Modo cavernicola: fecha | módulo | estado | problema | decisión

> Una línea por entrada. Sin explicaciones largas.
> Claude Code actualiza este archivo al terminar cada módulo.
> Diego y Juan lo leen antes de empezar cualquier sesión nueva.

---

## FORMATO
```
YYYY-MM-DD | MODULO | ESTADO | PROBLEMA ENCONTRADO | DECISIÓN TOMADA
```

Estados posibles: INICIADO · EN_CURSO · COMPLETO · BLOQUEADO · PARCIAL

---

## REGISTRO

| Fecha | Módulo | Estado | Problema | Decisión |
|---|---|---|---|---|
| 2026-07-03 | general (auditoría previa a implementar) | BLOQUEADO | backend/src/modules tiene 65 archivos .ts (usuario, animal, reproduccion, potrero, venta, gasto, reporte, alerta, notificacion) pero todos están en 0 bytes — es solo el scaffold de setup-estructura.sh, ningún módulo tiene código real. web/ (Angular) sí tiene modelos e infraestructura de API reales para todas las entidades. mobile/ y shared-contracts/ están vacíos igual que backend. Además, /configuracion/aprobacion y /sincronizacion/* del contrato no tienen ni carpeta de módulo creada en backend. | Se eligió empezar por usuario (auth+guards), dependencia dura de todos los demás módulos |
| 2026-07-03 | usuario | COMPLETO | 1) package.json del backend solo tenía el starter de Nest — se instalaron TypeORM+pg, @nestjs/jwt+passport-jwt, bcrypt, class-validator/transformer, @nestjs/config, @types/pg, dotenv. 2) app.module.ts importaba ./app.controller y ./app.service que nunca existieron (bug del scaffold) — se quitaron. 3) correo es UNIQUE(finca_id, correo) en schema.sql, no globalmente único, pero /auth/login del contrato no recibe fincaId — login busca por correo cross-finca; con 1 sola finca en el piloto no colisiona, pero es limitación conocida si se agregan fincas con usuarios de correo repetido. 4) GET /usuarios y finca-tenant.guard no tenían rol/propósito definido en contrato ni CLAUDE.md — se decidió con Diego/Juan: GET /usuarios solo dueno_finca; FincaTenantGuard verifica usuario.activo=true en BD en cada request. 5) Guards registrados como APP_GUARD globales (con @Public() como escape hatch) en vez de @UseGuards() por endpoint. 6) HttpExceptionFilter tenía 2 bugs propios encontrados en las pruebas: devolvía error:"BAD_REQUEST" (SCREAMING_CASE) en vez de "Bad Request", y devolvía error:"Internal Server Error" hardcodeado para excepciones sin mensaje custom (ej. UnauthorizedException() vacía) sin importar el statusCode real — ambos corregidos usando http.STATUS_CODES de Node. 7) La BD real es Supabase (las 13 tablas de schema.sql ya existen ahí, confirmado por query a information_schema) — NO se necesitó migración de TypeORM. DATABASE_URL va en backend/.env (gitignored), no en la raíz — corregido porque backend/ es el cwd real al correr npm run start. | Probado end-to-end contra Supabase real: POST /auth/login, GET /usuarios (200 con dueno_finca, 403 con administrador_finca — RolesGuard funcionando), POST /usuarios (201, y 400 por correo duplicado), sin token (401), token inválido (401). Seed script en backend/src/scripts/seed.ts (npx ts-node -r tsconfig-paths/register src/scripts/seed.ts) crea finca+usuario de prueba idempotente. test/app.e2e-spec.ts sigue desactualizado (testeaba GET / que ya no existe) — pendiente. docker-compose.yml quedó armado como fallback local pero no se usó (se probó directo contra Supabase). |
| 2026-07-03 | animal | COMPLETO | 1) AnimalResponse necesita potreroActualId (movimiento_ganado, PotreroModule) y enGestacion/conteoReproduccion (reproduccion, ReproduccionModule) — ninguno existe todavía. Decisión: AnimalRepository NO hace queries cross-módulo; se devuelven placeholders documentados con TODO (potreroActualId=null, enGestacion=false, conteoReproduccion={0,0}) hasta que esos módulos existan y AnimalModule los importe. 2) El filtro "buscar" del contrato dice "codigo, raza o nombre parcial" pero animal no tiene columna nombre en schema.sql — se implementó ILIKE sobre codigo y raza únicamente. 3) El filtro potreroId de GET /animales se acepta en el DTO de query (para no romper el contrato) pero no se aplica — misma razón del punto 1. 4) Roles de /peso, /mortalidad, /foto no estaban en el contrato salvo /peso — se igualaron los 3 a dueno_finca+administrador_finca+veterinario. 5) docker-compose.yml solo tenía el servicio Postgres vacío de esquema — se agregó mount de doc/schema(1).sql como script de init (docker-entrypoint-initdb.d) para que levante con las 13 tablas ya creadas; no se pudo probar en vivo porque Docker Desktop no corría en este entorno, pero `docker compose config` valida sintaxis y rutas. 6) Se agregó numericTransformer compartido (shared/utils/) porque el driver pg devuelve NUMERIC como string — se reutiliza en animal.valor_comercial_* e historial_peso.peso_kg. 7) Incidente de seguridad menor: apareció un token FlyV1 (deploy de Fly.io) pegado por error en backend/.env — se removió; pertenece a GitHub Actions secrets (FLY_API_TOKEN) para .github/workflows/deploy-backend.yml, no a este archivo. | Probado end-to-end contra Supabase real: POST /animales (201), código duplicado (400), POST /peso (201) con numericTransformer devolviendo pesoActual como number (320.5, no string), GET /animales/{id} y GET /animales (batch query de pesos sin N+1) con pesoActual correcto, POST /mortalidad (200, transacción atómica mortalidad+animal.estado), segundo intento de mortalidad (409), estado final "muerto" persistido. Queda un registro de prueba VACA-001 (muerto) en la finca de seed de Supabase. |

| 2026-07-04 | configuracion | COMPLETO | 1) GET /configuracion/aprobacion no define qué pasa si la finca no tiene fila (finca nueva) — se decidió crear la fila con los defaults del schema (umbrales NULL, aplica a ambos) en vez de devolver 404. 2) El contrato no incluye configurado_por en la response — se persiste (viene del JWT en cada PATCH) pero no se expone. 3) Entorno de esta sesión sin backend/.env ni Docker corriendo — se probó con unit tests (repos mockeados); e2e contra BD real pendiente de DATABASE_URL. | GET abierto a todos los roles autenticados, PATCH solo dueno_finca. Service exportado para que Venta/Gasto lean la config al aplicar auto-aprobación. También en este commit: doc/CLAUDE.md des-duplicado, schema(1).sql renombrado a schema.sql, scaffold muerto de notificacion eliminado. |

| 2026-07-04 | reproduccion | COMPLETO | 1) El contrato no acepta fecha_probable_parto en el POST pero la columna existe y el dashboard usa proximosAParto — se calcula automáticamente como fecha + 283 días (gestación bovina promedio). 2) confirmar-parto no define roles en el contrato — se igualó al POST (dueno+admin+vet). 3) pesoNacimiento del becerro no tiene tabla propia — se registra como primera fila de historial_peso dentro de la misma transacción. 4) Para evitar dependencia circular AnimalModule↔ReproduccionModule, ReproduccionRepository registra las entities Animal/HistorialPeso directamente (forFeature) en vez de importar AnimalModule; regla acordada: cada módulo puede LEER entities ajenas, solo escribe las propias — con la única excepción de la genealogía (reproduccion crea el becerro dentro de su transacción, es el requisito de negocio). | Genealogía en una sola transacción: crear becerro (madre_id=vaca_id, padre_id=toro_id, categoria=becerro), peso de nacimiento opcional, becerro_resultante_id + estado=exitoso. Validaciones POST: toro macho / vaca hembra de la finca, ni muertos ni vendidos, 409 si la vaca tiene evento en_curso (respaldado por el constraint EXCLUDE). Placeholders de animal conectados: enGestacion (batch, sin N+1) y conteoReproduccion reales; queda pendiente solo potreroActualId. 9 unit tests verdes. |

| 2026-07-04 | potrero | COMPLETO | 1) GET /potreros del contrato devuelve array plano sin paginación (a diferencia del resto de listados) — se respetó el contrato tal cual. 2) "potrero actual" de un animal no es columna: se define como potrero_destino_id del último movimiento (fecha DESC, created_at DESC como desempate) — DISTINCT ON en batch para el listado. 3) Roles de POST /potreros/movimientos no definidos en contrato — se dio a dueno+admin+vet (es acción de campo, igual que peso). 4) El filtro potreroId de GET /animales se resuelve primero a lista de ids de animales y luego se pasa al repositorio de animal — el service de animal ahora consume PotreroRepository y ReproduccionRepository. | CRUD potrero (nombre único por finca → 400) + movimientos (origen≠destino → 400, animal y ambos potreros validados contra la finca). Conectados los dos placeholders restantes de animal: potreroActualId y filtro potreroId — no quedan TODOs en el módulo animal. 5 unit tests verdes. |

| 2026-07-05 | venta | COMPLETO | 1) Auto-aprobación por tiempo sin cron: evaluación perezosa — un UPDATE (pendientes con created_at vencido → aprobado/por_tiempo/auto_aprobado=true) corre al inicio de GET /ventas, GET /gastos y antes de aprobar/rechazar; el índice parcial idx_venta_pendientes lo hace barato. Si el piloto necesita puntualidad de minutos, migrar a @nestjs/schedule (marcado con comentario ponytail en venta.repository.ts). 2) soloMisAprobaciones: con la evaluación perezosa aplicada, "las que debo resolver (pendientes y no vencidas)" = simplemente estado_aprobacion='pendiente'. 3) Roles de POST /ventas no definidos en contrato — se dio a dueno+admin (mismos del GET). 4) En rechazo se guarda aprobado_por=quien rechazó (auditoría de quién resolvió); el contrato no define ese detalle. 5) DECISIÓN DE NEGOCIO ABIERTA: ¿una venta aprobada con animalId debe marcar el animal como 'vendido'? El contrato no lo dice — NO se implementó, anotado abajo. | Auto-aprobación por monto leída SIEMPRE de configuracion_aprobacion (nunca hardcode): monto < umbral y aplica_a_ventas → aprobado/por_monto/auto_aprobado=true permanente; umbral NULL desactiva. Aprobar/rechazar solo dueno_finca y solo sobre pendientes (409 en cualquier otro estado — imposible revertir un auto_aprobado). 10 unit tests verdes. |

| 2026-07-05 | gasto | COMPLETO | Sin problemas nuevos: mismo flujo de aprobación que venta, reutilizando evaluarAutoAprobacionPorMonto (shared/utils/aprobacion.util.ts) y el mismo UPDATE perezoso de vencidos (lee aplica_a_gastos). RechazarDto se reutiliza desde venta/dto. Roles: GET/POST dueno+admin, aprobar/rechazar solo dueno (igual que venta). | 4 unit tests verdes (umbral, aplica_a_gastos=false, vencidos, 409 sobre resuelto). |

| 2026-07-05 | reporte | COMPLETO | El contrato no define las fórmulas de los KPIs — decisiones tomadas (revisables por Diego/Juan): 1) "hato" = animales activo+en_tratamiento (muertos/vendidos fuera de conteos, peso promedio y valor). 2) tasaNatalidad = partos exitosos últimos 12 meses / vacas del hato ×100. 3) tasaMortalidad = muertes últimos 12 meses / (hato+esas muertes) ×100. 4) ingresos/gastosMes = SUM de aprobados con fecha del mes calendario actual. 5) porcentajeAutoAprobado = auto_aprobado=true / total transacciones (ventas+gastos) ×100. 6) cambiosEstadoSalud: no existe tabla de eventos de salud — se reporta el nº de animales actualmente en_tratamiento como proxy (inconsistencia anotada abajo). 7) proximosAParto = en_curso con fecha_probable_parto ≤ hoy+30 días. 8) En ingresos-vs-gastos el filtro categoria solo restringe gastos (los ingresos siempre completos); pendientes/rechazadas se listan pero no suman al balance. | Solo lectura, dueno+admin. El dashboard corre los vencimientos por tiempo de ventas y gastos antes de contar pendientes. Agregaciones en SQL directo (FILTER/DISTINCT ON), siempre con finca_id. 2 unit tests verdes. |

| 2026-07-05 | alerta | COMPLETO | El contrato solo define GET /alertas y marcar-leida — no dice CUÁNDO se generan alertas. Decisión (revisable): se genera alerta severidad 'media' cuando una venta o gasto queda pendiente de aprobación (es lo que el Dueño necesita ver en la campanita); las auto-aprobadas no generan alerta. AlertaService.crear() es hook interno, no endpoint. No se generan aún alertas de origen 'animal' (¿mortalidad? ¿capacidad de potrero superada?) — decisión de negocio anotada abajo. | GET paginado con filtros tipoOrigen/leida + PATCH marcar-leida (404 si no es de la finca), dueno+admin. Venta y Gasto disparan la alerta al crear pendientes (verificado en sus specs: pendiente → alerta, auto-aprobada → sin alerta). Con esto los 9 módulos del backend del contrato v2 están COMPLETOS (falta solo sincronizacion, fase móvil). |

| 2026-07-05 | deploy | EN_CURSO | 1) develop ya tenía fly.toml (app=farmtec, región gru) y Dockerfile de un deploy previo — se mergeó origin/develop a la feature branch. 2) El merge de main.ts vino con sintaxis rota (void bootstrap() dentro de la función) — corregido. 3) fly.toml exige health check GET /api/v1/health que no existía — se agregó HealthController @Public() como infraestructura (no es endpoint de negocio del contrato). 4) Se agregó .dockerignore (no existía: la imagen copiaba node_modules y .env). | Deploy vía GitHub Actions al push a develop (FLY_API_TOKEN en secrets). Ruta: push de feature/setup-monorepo + PR a develop. Los secrets DATABASE_URL/JWT_SECRET deben existir en la app farmtec de Fly (hubo un deploy exitoso el 2026-07-04, se asume que están). |

| 2026-07-05 | venta+animal+alerta (decisiones de negocio) | COMPLETO | Diego resolvió las 3 decisiones abiertas: 1) venta aprobada con animalId SÍ marca el animal como 'vendido' — implementado en las 3 rutas de aprobación (por_monto al crear, directa al aprobar, por_tiempo en el UPDATE perezoso con RETURNING animal_id), siempre venta+animal en la misma transacción; 'vendido' solo desde estados vivos (nunca pisa 'muerto') y es terminal; vender un animal ya vendido/muerto → 400. 2) Alertas de origen 'animal': solo mortalidad registrada (severidad alta) para el piloto — capacidad de potrero superada es Fase 2. 3) cambiosEstadoSalud del dashboard: el proxy "animales actualmente en_tratamiento" queda como DECISIÓN TEMPORAL del piloto — revisar si se agrega tabla de eventos de salud en Fase 2. | 36 unit tests verdes (3 nuevos: animal vendido/muerto no se vende dos veces, aprobar pasa por la ruta transaccional, mortalidad genera alerta y no duplica sobre muertos). |

| 2026-07-06 | animal (reactivar) | COMPLETO | Diego pidió una forma de revertir un registro de mortalidad hecho por error. El contrato original decía "no reversible" — CAMBIO DE CONTRATO explícito: se agregó PATCH /animales/{id}/reactivar (nuevo endpoint, no existía). Decisiones: 1) exclusivo dueno_finca. 2) motivo obligatorio en el body (queda solo en el 200 de respuesta / logs, no se persiste en una tabla de auditoría separada — no existe esa tabla en schema.sql, revisar si Fase 2 la necesita). 3) el registro de mortalidad viejo se BORRA al reactivar (no se guarda como historial) — si el animal muere de nuevo después, crea un registro nuevo sin problema con el UNIQUE(animal_id) existente; no se tocó schema.sql. 4) 409 si el animal no está en estado 'muerto' cuando se pide reactivar. | Backend completo (dto+repository+service+controller, 2 tests nuevos en animal.service.spec.ts, 38/38 verdes). Frontend: botón "Reactivar (fue un error)" + modal en la ficha de animal, gateado a `esDueno() && estado==='muerto'`. |
| 2026-07-06 | potreros (frontend) | COMPLETO | Los modelos frontend de potrero/movimiento-ganado (heredados del scaffold) tenían el mismo patrón de bugs que otros módulos: PotreroCreateRequestModel/UpdateRequestModel incluían fincaId y estado (ninguno lo acepta CrearPotreroDto — fincaId sale del JWT, estado no es seteable por el cliente); MovimientoGanadoCreateRequestModel incluía fincaId y registradoPor (ambos derivados server-side del JWT); PotreroFilterModel no tenía sentido porque GET /potreros no acepta query params en absoluto (se eliminó el archivo); PotreroModel/MovimientoGanadoModel tenían createdAt y registradoPor que PotreroResponse/MovimientoResponse no exponen. Se corrigieron los 6 archivos de potrero/ y los 3+1(nuevo filter) de movimiento-ganado/ para reflejar exactamente PotreroResponse y MovimientoResponse del contrato. | Vista con 2 tabs (Potreros / Movimientos) en una sola ruta 'potreros', reutilizando ListadoComponent+MovimientoComponent como hijos de PotrerosComponent (@if/@else). "Nuevo potrero"/"Editar" gateado a dueno+admin; "Registrar movimiento" gateado a dueno+admin+veterinario (ROLES_CAMPO). Probado end-to-end contra backend local real (Supabase): crear potrero (201), editar potrero (200 PATCH), registrar movimiento con selects de animal/potrero poblados desde AnimalService/PotreroService (201 POST /potreros/movimientos), tabla de movimientos resuelve animalId/potreroOrigenId/potreroDestinoId a código/nombre legible. Build development limpio. |
| 2026-07-06 | reproduccion (frontend) | COMPLETO | Mismo patrón de bugs: ReproduccionCreateRequestModel incluía fincaId/fechaProbableParto/estado/becerroResultanteId (ninguno lo acepta CrearReproduccionDto — fechaProbableParto y becerroResultanteId son 100% calculados server-side); ReproduccionUpdateRequestModel no tenía sentido porque no existe PATCH /reproducciones/:id genérico — solo PATCH /reproducciones/:id/confirmar-parto — se eliminó y se reemplazó por ReproduccionConfirmarPartoRequestModel (resultado + becerro anidado, mismo shape que ConfirmarPartoDto/BecerroDto). ReproduccionFilterModel tenía search/id/fincaId/toroId/tipo que ListarReproduccionesQueryDto no soporta — solo estado+vacaId+pagina+limite son filtros reales. A diferencia de potrero/movimiento, ReproduccionResponse SÍ expone fincaId y createdAt tal cual, así que ReproduccionModel los mantuvo. | Vista única con filtro por estado (Todos/en_curso/exitoso/fallido), tabla resolviendo toroId/vacaId a código de animal vía AnimalService, "Nuevo registro" (selects de toro/vaca separados por sexo) y "Confirmar parto" por fila en_curso — ambos gateados a ROLES_CAMPO (dueno+admin+veterinario). El subformulario de becerro solo aparece si resultado=exitoso. Probado end-to-end contra backend local real: crear evento (201 POST /reproducciones, fechaProbableParto calculada correctamente a fecha+283 días), confirmar parto exitoso con becerro (200 PATCH /reproducciones/{id}/confirmar-parto), verificado que el becerro se creó de verdad en /animales (categoria=becerro, sexo y peso de nacimiento correctos). Build development limpio. |
| 2026-07-06 | venta (frontend) | COMPLETO | Mismo patrón de bugs: VentaCreateRequestModel incluía fincaId/estadoAprobacion/tipoAprobacion/autoAprobado/creadoPor/aprobadoPor/motivoRechazo (ninguno lo acepta CrearVentaDto — todos son calculados o derivados del JWT server-side); VentaUpdateRequestModel no tenía sentido porque no existe PATCH /ventas/:id genérico — solo aprobar (sin body) y rechazar (RechazarDto) — se eliminó y se reemplazó por VentaRechazarRequestModel ({motivo?}). VentaFilterModel tenía search/id/fincaId/animalId/comprador/tipoAprobacion que ListarVentasQueryDto no soporta — los filtros reales son estadoAprobacion/soloMisAprobaciones/fechaInicio/fechaFin+paginación. Se eliminó VentaSummaryModel (igual que en reproduccion, VentaResponse es una sola forma — no hay split lista/detalle real en el contrato). | Vista única con filtro por estado (Todos/pendiente/aprobado/rechazado), tabla resolviendo animalId a código vía AnimalService (— si no hay animal asociado), "Nueva venta" abierta a ambos roles financieros (dueno+admin, mismo gate que la ruta) con selector de animal restringido a estado activo/en_tratamiento (replica la validación real del backend que rechaza vendido/muerto), "Aprobar"/"Rechazar" por fila pendiente gateados a solo dueno_finca (los únicos roles con @Roles(DUENO_FINCA) en esos 2 endpoints). Probado end-to-end: crear venta sin animal por monto alto (201, quedó pendiente por exceder umbral), aprobar (200 PATCH .../aprobar), crear venta con animal por monto bajo (201, también pendiente — la finca de prueba no tiene umbral de auto-aprobación activo, comportamiento correcto del backend no un bug), aprobar y confirmar que el animal pasó a estado 'vendido' en /animales (regla de negocio cruzada venta→animal funcionando), verificado que el Dashboard agrega correctamente los ingresos del mes ($5.800.000 = suma exacta de las 2 ventas aprobadas). Rechazo verificado contra el backend real vía curl directo (PATCH /ventas/{id}/rechazar → estadoAprobacion:"rechazado", motivoRechazo persistido) y confirmado visualmente en la tabla tras recargar — el flujo de UI de rechazo no se pudo re-probar por clic debido a inestabilidad recurrente de la pestaña de preview (documentada en sesiones anteriores), pero el patrón de modal+PATCH es idéntico al ya probado en reproduccion/confirmar-parto y animal/mortalidad. Build development limpio. |
| 2026-07-06 | reproduccion (validaciones de negocio adicionales) | COMPLETO | Diego pidió reforzar 2 reglas de negocio que el contrato no exigía explícitamente pero que son necesarias: 1) un becerro (categoria='becerro') no está en edad reproductiva — hasta ahora ReproduccionService.create solo validaba sexo (toro=macho, vaca=hembra) y estado (no muerto/vendido), pero un becerro macho o una becerra podían registrarse como padres. 2) la fecha del evento (monta/inseminación) no puede ser futura — antes solo se validaba el formato (@IsDateString), no que fuera <= hoy. Confirmado con Diego: la regla del becerro aplica a AMBOS animales (toro y vaca), no solo a la vaca. | Agregado en reproduccion.service.ts (create()): el loop existente sobre [toro, vaca] ahora también rechaza categoria=BECERRO (400); comparación de strings ISO (dto.fecha > hoy) para la fecha futura (400) — no se tocó el DTO porque class-validator no tiene un decorador nativo simple para "fecha <= hoy dinámico", se resolvió en el service. 3 tests nuevos en reproduccion.service.spec.ts (toro becerro, vaca becerra, fecha futura) — 12/12 verdes, 41/41 en todo el backend. Frontend: el select "Toro" en reproduccion.component.ts filtraba por sexo==='macho' (bug expuesto por esta validación — un becerro macho aparecía como opción válida de toro); se corrigió a categoria==='toro' (y vacas a categoria==='vaca', excluyendo becerras del select también). Se agregó [max]="hoy()" al input de fecha para bloquear fechas futuras nativamente en el navegador, con el backend como fuente de verdad. Verificado end-to-end contra backend real (hubo que reiniciarlo para tomar el código nuevo — corría con `nest start` sin watch, se relanzó con `start:dev`): ambas reglas devuelven 400 con los mensajes esperados; quedaron 2 registros de prueba inválidos en la BD de desarrollo creados ANTES del reinicio (ids aa89da94.../44dc381c...) que no se pudieron borrar por no existir DELETE /reproducciones en el contrato — data de prueba inofensiva, no afecta producción. Adicionalmente Diego confirmó que las otras 3 "mejoras" pedidas ya estaban cubiertas y no requirieron cambios de backend: potrero (nombre único por finca vía existsNombreEnFinca, hectareas/capacidadEstimada positivas vía @IsPositive en create-potrero.dto.ts — ambas ya existían desde el módulo original; solo se agregó Validators.min(0.01/1) en el formulario frontend para feedback inmediato sin round-trip), venta (monto positivo ya validado por @IsPositive en CrearVentaDto) y animal (sin cambios, confirmado explícitamente que no hacía falta ninguna mejora). |
| 2026-07-06 | reporte + reproduccion (dashboard: vivos/muertos, inseminación) | COMPLETO | Diego pidió 3 mejoras al Dashboard/reproducción: 1) separar en el dashboard el total de animales vivos de los muertos — DashboardResponse no tenía ningún campo para el conteo histórico de muertos (solo existía `tasaMortalidad` sobre los últimos 12 meses). 2) "pesos registrados" en Actividad Reciente contaba TODOS los pesajes de historial_peso de los últimos 7 días sin filtrar por si el animal sigue vivo — un pesaje de un animal ya muerto/vendido no debería contar como actividad vigente del hato. 3) la validación de creación de reproduccion.service.ts exigía toro Y vaca vivos siempre — mal para inseminación artificial, que puede usar semen ya almacenado de un toro que después murió o se vendió; la vaca sí o sí debe estar viva (gesta físicamente), el toro no. | CAMBIO DE CONTRATO (con permiso explícito de Diego): se agregó `animalesMuertos: integer` a DashboardResponse en contrato-api-v2.yaml (conteo histórico completo de estado='muerto', no solo 12 meses) — nuevo método ReporteRepository.totalMuertos(). `pesajes7Dias()` ahora hace JOIN con animal y filtra estado IN ('activo','en_tratamiento') — mismo criterio que "hato" en el resto del dashboard. En reproduccion.service.ts: se separó el chequeo de estado que antes iteraba [toro, vaca] igual para ambos — la vaca siempre rechaza muerto/vendido; el toro solo rechaza muerto/vendido cuando tipo='monta_natural' (inseminación permite toro no vivo). Frontend: DashboardModel + KPI card nueva "Animales Muertos" (icono LucideSkull, junto a "Total Animales"). 1 test nuevo en reporte.service.spec.ts (animalesMuertos), 3 tests nuevos en reproduccion.service.spec.ts (toro muerto rechazado en monta natural, toro muerto/vendido permitido en inseminación, vaca muerta rechazada incluso en inseminación) — 44/44 verdes en todo el backend. Verificado end-to-end contra backend real (reiniciado de nuevo para tomar el código — sigue en modo `start:dev` desde el cambio anterior): GET /reportes/dashboard devuelve animalesMuertos correctamente y se ve en el KPI card; POST /reproducciones con tipo=monta_natural y toro muerto → 400; mismo toro con tipo=inseminacion → 201 con fechaProbableParto calculada correctamente. Build frontend limpio. |
| 2026-07-06 | venta + gasto + potrero + animal (flujo de aprobación del dueño) | COMPLETO | Diego pidió que el dueño sea el único que autoriza lo que hacen administrador/veterinario, con 3 casos concretos: 1) venta/gasto creados por administrador_finca deben requerir SIEMPRE aprobación manual del dueño, nunca auto-aprobarse (ni por monto ni por tiempo vencido) — hoy evaluarAutoAprobacionPorMonto y autoAprobarVencidas/autoAprobarVencidos no distinguían quién creó el registro. 2) movimientos de potrero deben notificar al dueño (alerta informativa, NO requiere su aprobación — es de único paso). 3) el administrador no puede reactivar un animal de muerto directamente (ya era así, exclusivo dueno_finca), pero debe poder SOLICITAR la reactivación al dueño para que la revise. | CAMBIOS DE CONTRATO (con permiso explícito): (a) nuevo endpoint POST /animales/{id}/solicitar-reactivacion (solo administrador_finca) — NO cambia el estado del animal, solo crea una alerta tipo 'animal' severidad alta notificando al dueño; el dueño sigue usando el PATCH .../reactivar existente (sin cambios) para aplicar el cambio real él mismo. (b) nuevo valor 'potrero' en el enum TipoOrigenAlerta — requirió ALTER TYPE tipo_origen_alerta ADD VALUE 'potrero' ejecutado en vivo contra Supabase (aditivo, confirmado explícitamente por Diego antes de ejecutar). venta.service.ts/gasto.service.ts: create() ahora recibe rolCreador (JWT) — si es administrador_finca, fuerza {pendiente/pendiente/false} ignorando el umbral por monto; venta.repository.ts/gasto.repository.ts: autoAprobarVencidas/autoAprobarVencidos ahora excluyen con NOT IN (SELECT id FROM usuario WHERE rol='administrador_finca') — nunca se auto-aprueban por tiempo tampoco. potrero.service.ts: createMovimiento() ahora inyecta AlertaService y genera alerta informativa (severidad media, sin bloquear la operación) con el código del animal y los nombres de ambos potreros. Frontend: botón "Solicitar reactivación al dueño" en la ficha de animal, visible solo a administrador_finca cuando estado='muerto', con su propio modal (muestra confirmación "Solicitud enviada" sin tocar el estado del animal en pantalla). 2 tests nuevos en animal.service.spec.ts, 1 en venta.service.spec.ts, 1 en gasto.service.spec.ts, 1 actualizado en potrero.service.spec.ts (verifica alertaService.crear) — 48/48 verdes en todo el backend. Probado end-to-end con un usuario administrador_finca real (admin@ganadosmart.test, ya existente de una sesión previa): venta de $1.000 creada por admin quedó pendiente (antes se auto-aprobaba para cualquier rol); gasto de $100 creado por admin también pendiente; admin intentó PATCH .../reactivar directo → 403; admin usó solicitar-reactivacion → 201 y el animal siguió en estado 'muerto'; movimiento de potrero generó alerta con mensaje legible ("VACA-VIVA-001 se movió de Potrero Norte a Potrero Sur"); flujo completo verificado también por UI (login como admin, botón visible, modal, confirmación). Build frontend limpio. |
| 2026-07-06 | reporte + reproduccion + animal (correcciones menores) | COMPLETO | Diego reportó 4 cosas menores tras revisar el Dashboard/Reproducción/Animales: 1) el KPI "Inseminaciones" del dashboard no aclaraba qué contaba — investigado: era un bug real, la query contaba TODOS los eventos reproductivos de los últimos 7 días (monta_natural + inseminacion) etiquetados como "inseminaciones", sin filtrar por tipo. 2) "Próximos a parto" y "En tratamiento" en 0 — verificado con los datos de prueba actuales: es correcto, no es bug (fechaProbableParto de los registros de prueba caen ~9 meses adelante, fuera de la ventana de 30 días; ningún animal de prueba tiene estado='en_tratamiento' — ese KPI es el proxy documentado de "cambios de estado de salud" ante la ausencia de tabla de eventos de salud). 3) en la ficha de animal no había forma de ver/cambiar la foto. 4) en Reproducción, algunas filas mostraban el UUID crudo del toro/vaca en vez del código — investigado: reproduccion.component.ts solo cargaba animales con estado='activo' (limite 100) para resolver el código, así que cualquier toro/vaca ya muerto o vendido en un registro histórico no se encontraba y caía al fallback `?? id`. | reporte.repository.ts: conteosReproduccion() ahora filtra `WHERE tipo = 'inseminacion'` en el COUNT de los últimos 7 días (antes contaba ambos tipos). reproduccion.component.ts: se cambió el fetch de animales a sin filtro de estado (igual que ventas/gastos) para que codigoAnimal() resuelva TODOS los animales referenciados sin importar su estado actual; los selects de "Nuevo registro" (toros/vacas) ahora filtran explícitamente por categoria Y estado IN (activo, en_tratamiento) directamente en el computed, ya que antes dependían del filtro del fetch. Animal: nuevo modal "Foto del animal" en la ficha (botón "Agregar foto"/"Cambiar" superpuesto en la imagen, visible a ROLES_CAMPO — mismo gate que PATCH /animales/{id}/foto) — el contrato solo acepta una URL (ActualizarFotoDto.fotoUrl), no hay almacenamiento de archivos (S3/Supabase Storage) en el backend, así que es un campo de texto para pegar una URL ya alojada, no un file-picker nativo; esto se explicó en el propio modal para no prometer algo que el sistema no soporta. Nuevo modelo AnimalFotoRequestModel + AnimalService.actualizarFoto(). Sin cambios de contrato ni de tests unitarios (son ajustes de SQL/frontend, no de reglas de negocio nuevas) — 48/48 backend sigue verde. Verificado end-to-end: dashboard "Inseminaciones" pasó de 6 a 2 (el conteo real de tipo=inseminacion en los últimos 7 días); tabla de Reproducción ahora muestra "TORO-MUERTO-TEST"/"VACA-001" en vez de UUIDs; foto subida por URL se renderiza correctamente en la ficha y el botón cambia a "Cambiar". Build frontend limpio. |
| 2026-07-06 | animal + reproduccion (foto por archivo + pajilla externa) | COMPLETO | Diego pidió 2 mejoras: 1) en la ficha de animal, poder subir la foto por archivo además de por URL. 2) en inseminación, poder usar semen de una pajilla externa (proveedor+raza) cuando no se usa un toro propio de la finca — antes toroId era siempre obligatorio. | CAMBIOS DE SCHEMA (con permiso explícito, ejecutados en vivo contra Supabase): tabla reproduccion — toro_id ahora nullable, se agregaron columnas pajilla_proveedor/pajilla_raza (VARCHAR), y un CHECK constraint chk_repro_origen_semen que exige: monta_natural siempre con toro_id (sin pajilla); inseminacion con EXACTAMENTE una de las dos (toro_id solo, o pajilla_proveedor+pajilla_raza juntos, nunca ambos ni ninguno). reproduccion.service.ts reescrito: la validación de campos ahora ocurre antes de tocar la BD (evita queries innecesarias), solo busca el animal-toro si dto.toroId viene presente. CrearReproduccionDto: toroId ahora opcional, se agregan pajillaProveedor/pajillaRaza opcionales. 5 tests nuevos en reproduccion.service.spec.ts (pajilla exitosa, monta_natural+pajilla rechazada, toroId+pajilla simultáneos rechazado, ninguno de los dos rechazado, pajilla incompleta rechazada) — 20/20 en el módulo. Frontend: reproduccion.component.ts tiene un toggle "Toro de la finca"/"Pajilla externa" que solo aparece cuando tipo=inseminacion; la columna "Toro" de la tabla se renombró a "Origen semen" y muestra el código del toro O "Pajilla — {proveedor} ({raza})". | CAMBIO DE INFRAESTRUCTURA (con permiso explícito): se instaló @supabase/supabase-js (nueva dependencia) y se creó el bucket público "animal-fotos" en el proyecto Supabase existente (mismo usado para Postgres — no es un proveedor nuevo), límite 5MB, solo JPEG/PNG/WEBP. Nuevo SupabaseStorageService (shared/services/) inyectado en AnimalModule. Nuevo endpoint POST /animales/{id}/foto/upload (ROLES_CAMPO, FileInterceptor de @nestjs/platform-express, multer en memoria) que sube el archivo a Storage, obtiene la URL pública y la persiste en foto_url (reutilizando animalRepository.actualizarFoto) — PATCH /animales/{id}/foto (por URL) queda sin cambios, ahora son dos caminos alternativos al mismo campo. Frontend: modal de foto ahora tiene un toggle "Pegar URL"/"Subir archivo"; nuevo AnimalService.subirFoto() usando FormData + ApiClientService.upload(). BUG REAL ENCONTRADO Y CORREGIDO en el camino: RequestBuilder.buildBody() nunca revisaba si el body YA era una instancia de FormData — cuando isMultipart=true, siempre intentaba reconstruir un FormData iterando Object.entries(body), pero Object.entries() sobre un FormData real devuelve un array vacío (no son propiedades enumerables), así que el archivo nunca llegaba al backend (confirmado con un 400 "Debe adjuntar un archivo de imagen" en la primera prueba real). Corregido con un chequeo `if (body instanceof FormData) return body;` al inicio del método — este bug también habría afectado a cualquier futuro upload que construyera su propio FormData antes de llamar a apiClient.upload(). 53/53 tests verdes en todo el backend. Verificado end-to-end: pajilla creada por UI (toggle + campos proveedor/raza) aparece correctamente en la tabla; archivo subido por UI se sube a Supabase Storage real y la URL pública se renderiza en la ficha del animal. Build frontend limpio. |

| 2026-07-06 | reporte (GET /reportes/actividad) + reproduccion (creado_por) | COMPLETO | Nuevo endpoint de historial/auditoría "quién hizo qué", exclusivo dueno_finca. Sin tabla nueva: UNION ALL de 8 tablas existentes (animal, historial_peso, mortalidad, reproduccion, potrero, movimiento_ganado, venta, gasto) que emite 13 tipos de evento. PROBLEMA: 4 de los 13 tipos pedidos referencian columnas de usuario que NO existían — animal, potrero y reproduccion no tenían creado_por, y parto_confirmado no tiene confirmado_por. Diego decidió (respuesta a la pregunta): agregar creado_por SOLO a reproduccion. CAMBIO DE SCHEMA (autorizado): ALTER TABLE reproduccion ADD COLUMN creado_por UUID REFERENCES usuario(id) ON DELETE SET NULL — reflejado en schema.sql y en la entity; el service lo llena desde usuario.sub (JWT) al crear. NOTA: el chk_repro_origen que Diego pegó en su respuesta NO se aplicó — choca con el chk_repro_origen_semen ya existente (que cubre correctamente la lógica de pajilla); solo se tomó el ADD COLUMN creado_por. Los otros 3 tipos (animal_creado, potrero_creado, parto_confirmado) se emiten con usuario=null (evento sin atribución, no filtrable por usuarioId) — documentado; parto_confirmado se fecha por la creación del becerro (que ocurre al confirmar) y muestra el código del becerro. Otra limitación conocida: venta/gasto no tienen columna de fecha de aprobación/rechazo — venta_aprobada/rechazada y gasto_aprobado/rechazado se fechan por created_at (fecha de creación, no de resolución); las auto-aprobadas se excluyen del log (aprobado_por IS NULL) porque no son acción de un usuario. | Filtros: usuarioId, tipo (IsIn de los 13), fechaInicio/fechaFin (rango inclusivo), pagina/limite (default 50). Paginación con COUNT(*) OVER() en una sola query. Respuesta { datos, meta } igual que el resto de listados; cada entrada { tipo, descripcion, entidadId, entidadCodigo, usuario:{id,nombre,rol}|null, fecha }. finca_id siempre del JWT en cada rama del UNION. 3 tests nuevos (2 actividad en reporte.service.spec, 1 de propagación de creado_por en reproduccion.service.spec) — 56/56 verdes, build limpio. PENDIENTE: la query SQL cruda NO se probó contra Supabase real en esta sesión (contraseña rotada + sin DATABASE_URL válido); los unit tests mockean el repo. El ALTER de creado_por DEBE ejecutarse en Supabase antes/junto al deploy o POST /reproducciones fallará (synchronize de TypeORM está apagado). Incidente aparte: el build estaba roto en este entorno por node_modules desincronizado (faltaban @supabase/supabase-js y @types/multer, ambos YA en package.json del commit de Juan) — se corrigió con npm install, no fue cambio de código. |

| 2026-07-06 | gastos (frontend) | COMPLETO | Pantalla features/gastos/registro/ estaba en scaffold vacío y sus modelos tenían el patrón viejo (gasto-summary/gasto-update-request + gasto-create-request con fincaId/estados/creadoPor que el cliente no envía) — mismo problema que Juan ya había corregido en venta. Se limpiaron los modelos al patrón de venta: create-request solo {categoria,monto,descripcion?,fecha}; nuevo gasto-rechazar-request {motivo?}; filter refleja los query params REALES de GET /gastos (categoria, estadoAprobacion, soloMisAprobaciones, paginación — NO hay filtros de fecha en gastos, a diferencia de ventas); se borraron gasto-summary y gasto-update-request; index actualizado. Nuevo GastoService (clon de VentaService). La ruta 'gastos' pasó de TempPlaceholderComponent al componente real. | Copia de ventas/registro con las diferencias pedidas: columna "Categoría" (no Comprador), sin columna Animal, chips de filtro por categoría (Todos/Insumos/Nómina/Veterinario/Otro) que filtran server-side vía el query param categoria, gráfico de barras horizontales por categoría (agrega client-side un lote de hasta 100 gastos — ponytail documentado; si crece, usar desglosePorCategoria de /reportes/ingresos-vs-gastos). Mismos badges de estado (pendiente/aprobado/rechazado + Auto) y botones Aprobar/Rechazar en línea gateados a solo dueno_finca. Modal de nuevo gasto con select de categoría + monto + fecha + descripción opcional. Conecta a GET/POST /gastos y PATCH /gastos/{id}/aprobar|rechazar. El sidebar ya tenía la entrada. Build development limpio (tsc + ng build). NO probado end-to-end contra backend (pendiente); NO commiteado (Diego pidió no commitear aún). |

| 2026-07-06 | usuarios (frontend) | COMPLETO | Pantalla features/usuarios/ estaba en scaffold vacío; modelos con el patrón viejo (usuario-create-request pedía contrasenaHash+fincaId — el backend hashea y toma fincaId del JWT; usuario-summary/usuario-update-request/usuario-filter con campos que GET /usuarios no soporta — solo acepta paginación). Se limpió: create-request ahora {nombre,correo,contrasena,rol} (= CrearUsuarioDto); filter solo {pagina,limite}; se borraron usuario-summary y usuario-update-request; index actualizado. UsuarioDetailModel se conservó (lo usan auth.service y login-response). Nuevo UsuarioService (listar paginado + crear). La ruta 'usuarios' pasó de TempPlaceholder al componente real (ya estaba gateada a roleGuard('dueno_finca')). | Tabla: avatar con iniciales + nombre + correo, Rol (badge con color por rol: dueño=verde, administrador=azul, veterinario=púrpura, consulta=gris), Estado (Activo=verde/Inactivo=gris), Último acceso (date | "Nunca"), y un menú ⋮. DECISIÓN: el ⋮ se renderiza DESHABILITADO con tooltip — el backend solo expone GET/POST /usuarios, no hay endpoints de editar/desactivar/eliminar, así que no hay acciones reales que ofrecer (no se inventó UI muerta). Botón "Invitar usuario" con modal (nombre, correo, contraseña min 8, select de rol) → POST /usuarios. Toda la pantalla es exclusiva dueno_finca (ruta + sidebar). Build development limpio. NO probado e2e; NO commiteado (Diego pidió no commitear). |

| 2026-07-06 | mortalidad (frontend + endpoint backend) | COMPLETO | La pantalla de Mortalidad pedía barras mensuales por categoría, torta de causas, KPI de causa principal y variación vs período anterior — todo eso necesita la FECHA y la CAUSA de muerte por animal, que NINGÚN endpoint exponía: GET /animales?estado=muerto devuelve el animal pero no sus datos de muerte (viven en la tabla mortalidad, sin endpoint de lectura), y GET /reportes/dashboard solo da tasaMortalidad (12m) y animalesMuertos (total). Se le presentó el bloqueo a Diego con 3 opciones; eligió agregar el endpoint. | CAMBIO DE CONTRATO (autorizado): nuevo GET /reportes/mortalidad — array (sin paginar, la tabla es chica) de {animalId, codigo, categoria, fecha, causa}, un ítem por animal muerto, JOIN mortalidad+animal, siempre con finca_id del JWT. DISPONIBLE A TODOS LOS ROLES autenticados (sin @Roles), igual que GET /animales y coherente con que el nav muestra "Mortalidad" a todos los roles — no es un KPI financiero restringido. Reflejado en contrato-api-v2.yaml y reporte.repository/service/controller; 1 test nuevo de mapping (6/6 en reporte, 57/57 en todo el backend, build limpio). Frontend features/mortalidad/: 4 KPIs (muertes 12m, tasa del dashboard, causa principal = moda de causas, variación 12m actuales vs 12m previos con color rojo/verde), barras mensuales apiladas becerros/adultos de los últimos 12 meses (agrupa por YYYY-MM de la fecha), torta de causas con CSS conic-gradient + leyenda (sin librería de charts), y tabla de registros. Ruta 'mortalidad' pasó de TempPlaceholder al componente real. Build development limpio. NO probado e2e contra backend real (el endpoint es nuevo, requiere deploy); NO commiteado (Diego pidió no commitear). NOTA: el endpoint GET /reportes/actividad de la sesión anterior sigue SIN reflejar en el contrato (esa tarea prohibía tocar el contrato) — pendiente de decisión si se documenta. |

| 2026-07-07 | mobile (app KMP completa) + sincronizacion (backend) | COMPLETO | Diego pidió la versión móvil "en Kotlin para iOS y Android conservando el diseño de la web", en rama aparte (feature/mobile-kmp). DECISIONES (confirmadas con Diego): 1) CAMBIO DE STACK MÓVIL: Kotlin Multiplatform + Compose Multiplatform reemplaza al React Native documentado — el scaffold RN de mobile/ (solo template, sin código de negocio) se eliminó; CLAUDE.md/PRD siguen diciendo React Native, PENDIENTE actualizarlos con permiso. 2) Paridad completa con la web (10 pantallas). 3) Offline CON sincronización — lo que obligó a construir el módulo backend sincronizacion que faltaba (el último del contrato v2). Matriz de versiones validada con build real ANTES de escribir la app: Kotlin 2.3.21 + Compose Multiplatform 1.11.1 + AGP 8.13.2 + Gradle 9.3.1 (cacheado de RN) + Ktor 3.5.1 + multiplatform-settings 1.3.0; los íconos son ImageVectors de trazo estilo Lucide dibujados a mano (material-icons-extended dejó de publicarse para CMP moderno). | MÓVIL (ganadosmart/mobile): composeApp con TODO el código (UI incluida) en commonMain — tema oscuro réplica del web (zinc + amber-600/emerald, badges con las mismas variantes, headings black), bottom nav (Dashboard/Animales/Reproducción/Potreros/Más) con el mismo gating por rol del sidebar web, y pantallas: Login, Dashboard (KPIs), Animales (listado con filtros/búsqueda paginada + ficha + crear + peso + mortalidad + reactivar dueño + solicitar reactivación admin + foto URL), Reproducción (crear con toro o pajilla externa + confirmar parto con becerro), Potreros (tabs potreros/movimientos + crear ambos), Ventas y Gastos (crear + aprobar/rechazar solo dueño + gráfico de categorías), Mortalidad (KPIs + barras mensuales becerros/adultos + torta de causas en Canvas), Usuarios (solo dueño + invitar), Alertas (filtros + marcar leída). OFFLINE: peso/mortalidad/movimiento capturan SinConexionException y encolan en SyncStore (JSON en Settings — ponytail: SQLDelight si crece); listas de animales/potreros se cachean para elegir sin señal; card de Sincronización en "Más" muestra la cola y dispara POST /sincronizacion/lote. BACKEND sincronizacion: entity accion_sync (la tabla YA existía en schema.sql/Supabase), POST /lote (ROLES_CAMPO, procesa en orden por timestampLocal, persiste cada acción para auditoría, reutiliza AnimalService/PotreroService — validaciones y alertas NO duplicadas; 409→conflicto con detalle, otros→error, sin frenar el lote) y POST /resolver-conflicto (usar_servidor descarta lo local; usar_local solo soportado para mortalidad — sobreescribe fecha/causa del registro existente; peso/movimiento no generan 409 hoy). PotreroService ahora se exporta del módulo. 8 tests nuevos (65/65 verdes, build backend limpio). APK debug compilado y verificado en esta máquina (13.9 MB). iOS: targets declarados + MainViewController + wrapper SwiftUI listos, pero Kotlin/Native SOLO compila targets Apple en macOS — iosApp/README.md documenta los pasos en una Mac; NO verificado aquí. NO probado contra backend real end-to-end (los endpoints de sincronizacion no están desplegados). NO commiteado. |

---

## INCONSISTENCIAS ABIERTAS
> Problemas entre contrato y schema que aún no tienen decisión.

- **animal.buscar / "nombre"**: contrato dice "busca por codigo, raza o nombre parcial" pero animal (schema.sql) no tiene columna nombre. Implementado sobre codigo+raza. Falta decidir si es error de redacción del contrato o si falta agregar el campo.
- ~~**alertas de origen 'animal'**~~: resuelto 2026-07-05 — solo mortalidad para el piloto (capacidad de potrero = Fase 2).
- ~~**cambiosEstadoSalud (dashboard)**~~: resuelto 2026-07-05 — proxy de animales en_tratamiento aceptado como decisión temporal del piloto.
- ~~**venta aprobada → ¿animal 'vendido'?**~~: resuelto 2026-07-05 — sí, en la misma transacción, en las 3 rutas de aprobación; estado terminal.
- ~~**AnimalResponse.potreroActualId / enGestacion / conteoReproduccion**~~: resueltos el 2026-07-04 (ReproduccionModule y PotreroModule conectados a animal).
- ~~**GET /animales?potreroId=**~~: resuelto el 2026-07-04 — filtra por animales cuyo último movimiento terminó en ese potrero.

---

## DECISIONES PERMANENTES
> Cosas que se decidieron una vez y no se vuelven a discutir.

| Fecha | Decisión |
|---|---|
| 2026-06-24 | nivel_escalamiento eliminado → tipo_aprobacion |
| 2026-06-24 | Roles: dueno_finca, administrador_finca, veterinario, usuario_consulta |
| 2026-06-24 | finca_id siempre del JWT, nunca del cliente |
| 2026-06-24 | auto_aprobado=true es permanente, nunca revertir |
| 2026-06-24 | codigo en Animal y Finca es asignable por usuario, id UUID es interno |
| 2026-07-06 | Mortalidad de animal SÍ es reversible vía PATCH /animales/{id}/reactivar (exclusivo dueno_finca, motivo obligatorio, borra el registro de mortalidad viejo) — reemplaza la nota anterior de "no reversible" |
| 2026-07-06 | El dueño es la única autoridad de aprobación sobre lo que crea administrador_finca: venta/gasto creados por admin NUNCA se auto-aprueban (ni por monto ni por tiempo); reactivar animal de muerto sigue exclusivo del dueño, el admin solo puede solicitarlo (POST /animales/{id}/solicitar-reactivacion → alerta, sin cambiar estado). Movimientos de potrero son la única excepción: solo notifican al dueño (alerta), no requieren su aprobación — son de único paso. |
| 2026-07-06 | Reproduccion.toroId es opcional: monta_natural siempre requiere toro propio; inseminacion admite EXACTAMENTE toro propio O pajilla externa (pajillaProveedor+pajillaRaza), nunca ambos ni ninguno — validado con CHECK constraint en BD (chk_repro_origen_semen) y en el service. |
| 2026-07-06 | Fotos de animal: dos caminos válidos al mismo campo foto_url — PATCH /animales/{id}/foto (URL de texto) o POST /animales/{id}/foto/upload (archivo real, sube a Supabase Storage bucket "animal-fotos", público, 5MB máx, JPEG/PNG/WEBP). Mismo proyecto Supabase que ya usamos para Postgres, no un proveedor nuevo. |
