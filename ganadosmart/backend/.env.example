PORT=3000

# Postgres — Supabase en el piloto, docker-compose (raíz del repo) en local
DATABASE_URL=postgresql://user:password@localhost:5432/ganadosmart

# JWT
JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN_SECONDS=28800
